---
# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/master-standalone-strict/deployment.json
apiVersion: apps/v1
kind: Deployment
metadata:
  name: &name electricilies
  annotations:
    reloader.stakater.com/auto: "true"
spec:
  template:
    spec:
      containers:
        - name: *name
          image: ghcr.io/electricilies/frontend:latest
          ports:
            - containerPort: 3000
          env:
            - name: NEXT_PUBLIC_BASE_URL
              value: backend.elecitricilies.com
            - name: AUTH_KEYCLOAK_ID
              value: frontend
            - name: AUTH_KEYCLOAK_ISSUER
              value: https://keycloak.example.com/realms/electricilies
            - name: AUTH_TRUST_HOST
              value: "true"
          envFrom:
            - secretRef:
                name: electricilies-keycloak
            - secretRef:
                name: electricilies-electricilies
          # readinessProbe: &health
          #   httpGet:
          #     path: /api/health
          #     port: 3000
          #   periodSeconds: 5
          #   failureThreshold: 10
          # livenessProbe:
          #   <<: *health
          #   periodSeconds: 10
          #   failureThreshold: 3
          resources:
            requests:
              cpu: 50m
              memory: 300Mi
            limits:
              cpu: 500m
              memory: 600Mi
  selector: {}
